# 百科全书式数据分析应用

一个基于 Streamlit 的数据分析和聚类分析应用，提供数据探索、KMeans 聚类、GMM 聚类和功能聚类等功能。

## 目录

- [项目结构](#项目结构)
- [主要特性](#主要特性)
- [安装和运行](#安装和运行)
- [架构设计](#架构设计)
- [优化历史](#优化历史)
- [开发指南](#开发指南)
- [扩展指南](#扩展指南)
- [依赖包](#依赖包)
- [许可证](#许可证)

## 项目结构

```
My_CD/
├── core/                    # 核心框架
│   ├── base/               # 基类和接口
│   │   ├── model_base.py    # 模型基类
│   │   ├── service_base.py  # 服务基类
│   │   └── page_base.py     # 页面基类
│   ├── exceptions.py        # 自定义异常
│   └── logger.py            # 日志系统
│
├── config/                   # 配置管理
│   ├── settings.py         # 应用设置
│   └── constants.py        # 常量定义
│
├── data/                     # 数据管理
│   └── data_manager.py     # 数据管理器
│
├── models/                   # 模型定义（按类型分类）
│   ├── clustering/         # 聚类模型
│   ├── classification/      # 分类模型（未来）
│   ├── regression/         # 回归模型（未来）
│   └── feature_selection/  # 特征选择（未来）
│
├── services/                 # 业务逻辑服务（按功能分类）
│   ├── clustering/         # 聚类服务
│   │   ├── kmeans_clustering.py
│   │   ├── gmm_clustering.py
│   │   └── functional_clustering.py
│   ├── classification/      # 分类服务（未来）
│   ├── regression/         # 回归服务（未来）
│   └── feature_selection/  # 特征选择服务（未来）
│
├── components/               # UI 组件（按功能分类）
│   ├── data/               # 数据处理组件
│   │   ├── file_loader.py
│   │   ├── data_transformation.py
│   │   └── clustering_data_prep.py
│   ├── viz/                # 可视化组件
│   │   ├── plot_hist_kde.py
│   │   ├── kmeans_result_viz.py
│   │   ├── gmm_result_viz.py
│   │   └── functional_clustering_viz.py
│   ├── clustering/         # 聚类相关组件
│   │   ├── params/         # 参数配置
│   │   ├── analysis/       # 分析工具
│   │   └── workflows/      # 工作流
│   ├── layout/             # 布局组件
│   └── utils/              # 工具组件
│
├── pages/                    # Streamlit 页面
│   ├── 01_Data_Exploration.py
│   ├── 02_test.py
│   └── 03_Clustering.py
│
├── utils/                    # 工具函数
│   └── state_manager.py    # 状态管理器
│
├── assets/                   # 静态资源
│   └── fonts/              # 字体文件
│
├── logs/                     # 日志文件（自动创建）
│
├── Homepage.py              # 主页面
├── state.py                 # 状态管理（向后兼容）
└── requirements.txt         # 依赖包
```

## 主要特性

### 1. 数据探索
- 支持 CSV、TXT、Excel 文件上传
- 数据描述统计
- 数据类型检查
- 缺失值分析
- 数据分布可视化（直方图和 KDE）

### 2. 聚类分析
提供三种聚类方法：

#### KMeans 聚类
- 数据转换（标准化、归一化等）
- 特征选择
- 参数配置（聚类数、迭代次数等）
- 迭代过程可视化
- 聚类结果可视化
- 肘部法则分析

#### GMM 聚类
- 数据转换
- 特征选择
- 参数配置（组件数、协方差类型等）
- 聚类结果可视化
- 肘部法则分析

#### 功能聚类
- 数据转换
- 参数配置
- 功能聚类结果可视化

## 安装和运行

### 1. 安装依赖

```bash
pip install -r requirements.txt
```

### 2. 运行应用

```bash
streamlit run Homepage.py
```

## 架构设计

本项目采用分层架构设计，具有清晰的模块划分和良好的可扩展性。架构设计考虑了未来添加新功能（特征选择、分类、回归等）的需求。

### 架构层次

#### 1. 核心层 (core/)

提供基础框架和通用功能：

- **base/**: 定义基类和接口
  - `BaseModel`: 所有模型的基类
  - `BaseService`: 所有服务的基类
  - `BasePage`: 所有页面的基类

- **exceptions.py**: 自定义异常类
  - `ModelError`: 模型相关错误
  - `DataError`: 数据相关错误
  - `ServiceError`: 服务相关错误
  - `ValidationError`: 验证相关错误

- **logger.py**: 日志系统
  - 统一的日志管理
  - 文件和控制台输出
  - 可配置的日志级别

#### 2. 配置层 (config/)

统一管理应用配置：

- **settings.py**: 应用设置
  - 路径配置
  - 页面配置
  - 默认参数

- **constants.py**: 常量定义
  - 模型类型
  - 算法列表
  - 方法选项

#### 3. 数据层 (data/)

数据管理：

- **data_manager.py**: 数据管理器
  - 统一的数据加载
  - 数据转换管理
  - 状态管理集成

#### 4. 模型层 (models/)

模型定义（按类型分类）：

- **clustering/**: 聚类模型
- **classification/**: 分类模型（未来）
- **regression/**: 回归模型（未来）
- **feature_selection/**: 特征选择（未来）

#### 5. 服务层 (services/)

业务逻辑实现（按功能分类）：

- **clustering/**: 聚类服务
- **classification/**: 分类服务（未来）
- **regression/**: 回归服务（未来）
- **feature_selection/**: 特征选择服务（未来）

#### 6. 组件层 (components/)

UI 组件（按功能分类）：

- **data/**: 数据处理组件
- **viz/**: 可视化组件
- **clustering/**: 聚类相关组件
- **layout/**: 布局组件
- **utils/**: 工具组件

#### 7. 页面层 (pages/)

Streamlit 页面：

- 每个页面对应一个功能模块
- 使用 `BasePage` 基类（可选）
- 统一的页面配置

### 设计原则

1. **分层架构**: 清晰的层次划分，每层职责明确，低耦合高内聚
2. **可扩展性**: 基类和接口设计，模块化组织，易于添加新功能
3. **可维护性**: 统一的代码风格，清晰的模块划分，完善的文档
4. **标准化**: 统一的命名规范，统一的接口设计，统一的错误处理

### 数据流

```
用户输入 → 页面层 → 组件层 → 服务层 → 模型层 → 数据层
                ↓
            状态管理
                ↓
            可视化组件
```

### 状态管理

使用 `StateManager` 统一管理 Streamlit 的 session_state：

- 统一的状态初始化
- 类型安全的状态访问
- 不同模块的状态隔离

### 错误处理

使用自定义异常类：

- `ModelError`: 模型相关错误
- `DataError`: 数据相关错误
- `ServiceError`: 服务相关错误
- `ValidationError`: 验证相关错误

### 日志系统

统一的日志管理：

- 文件日志：`logs/app_YYYYMMDD.log`
- 控制台输出
- 可配置的日志级别

### 配置管理

集中配置管理：

- `config/settings.py`: 应用设置
- `config/constants.py`: 常量定义
- 环境变量支持

## 优化历史

### 第一阶段：代码重构优化

#### 文件结构优化
- 重命名主文件：`🏠Homepage.py` → `Homepage.py`（移除 emoji，提高兼容性）

#### 架构改进
- 创建统一状态管理器 (`utils/state_manager.py`)
- 创建配置文件 (`config/`)
- 创建工具模块 (`utils/`)

#### 代码重构
- 创建通用布局组件 (`components/common_layout.py`)
- 创建聚类工作流组件 (`components/clustering_workflows.py`)
- 重构 Clustering 页面：从 343 行减少到约 40 行（减少 88%）

#### 模块导出优化
- 改进 `__init__.py` 文件，添加模块导出
- 优化字体配置使用统一配置

#### 开发工具
- 创建 `.gitignore` 文件
- 创建项目文档

**优化效果**:
- 代码重复率：从约 200+ 行重复代码减少到几乎为零
- 可维护性：模块化设计，便于维护和扩展
- 向后兼容：所有原有功能保持不变

### 第二阶段：组件目录重组

将 `components/` 目录下的 18 个文件按功能分类重组到子目录中：

#### 新的组件结构

```
components/
├── data/              # 数据处理组件
├── viz/               # 可视化组件
├── clustering/        # 聚类相关组件
│   ├── params/       # 参数配置
│   ├── analysis/     # 分析工具
│   └── workflows/    # 工作流
├── layout/           # 布局组件
└── utils/            # 工具组件
```

**优化效果**:
- 文件数量：从 18 个文件平铺 → 按功能分组到 5 个子目录
- 可维护性：相关功能集中，便于查找和修改
- 可扩展性：新功能可以轻松添加到对应子目录

### 第三阶段：架构全面优化

#### 核心框架 (core/)
- **基类系统**: `BaseModel`、`BaseService`、`BasePage`
- **异常处理**: 自定义异常类
- **日志系统**: 统一日志管理

#### 配置管理 (config/)
- `settings.py`: 应用设置和路径配置
- `constants.py`: 常量定义（算法列表、方法选项等）

#### 数据管理 (data/)
- `DataManager`: 统一的数据加载、转换和状态管理

#### 服务层重组 (services/)
- 按功能分类：`clustering/`、`classification/`、`regression/`、`feature_selection/`
- 所有聚类服务已移动到 `services/clustering/`

#### 模型层 (models/)
- 按类型分类：`clustering/`、`classification/`、`regression/`、`feature_selection/`
- 为未来扩展预留目录结构

**架构优势**:
- ✅ **清晰性**: 分层明确，职责清晰
- ✅ **标准化**: 统一的接口和规范
- ✅ **易维护**: 模块化设计，便于维护
- ✅ **易扩展**: 基类设计，预留扩展空间

## 开发指南

### 状态管理

使用 `StateManager` 类来管理状态：

```python
from utils.state_manager import StateManager

# 获取状态
data = StateManager.get("uploaded_df")

# 设置状态
StateManager.set("uploaded_df", df)

# 初始化所有状态
StateManager.init_session_state()
```

### 使用数据管理器

```python
from data.data_manager import DataManager

# 创建数据管理器
dm = DataManager(key_prefix="my_")

# 加载数据
df = dm.load_data(uploaded_file)

# 转换数据
transformed_df = dm.transform_data(df)

# 获取数据
data = dm.get_transformed_data()
```

### 组件导入方式

#### 方式 1: 从子模块导入（推荐）
```python
from components.data import load_data_file, render_data_transformation
from components.viz import plot_hist_kde
from components.clustering import render_kmeans_clustering_workflow
```

#### 方式 2: 从主模块导入（向后兼容）
```python
from components import (
    load_data_file,
    plot_hist_kde,
    render_data_transformation,
)
```

#### 方式 3: 直接导入（详细路径）
```python
from components.data.file_loader import load_data_file
from components.viz.plot_hist_kde import plot_hist_kde
```

### 服务导入方式

```python
# 从聚类服务导入
from services.clustering.kmeans_clustering import perform_kmeans_clustering
from services.clustering.gmm_clustering import perform_gmm_clustering

# 或从主模块导入（向后兼容）
from services import perform_kmeans_clustering
```

### 配置导入方式

```python
from config.settings import PAGE_CONFIG, SUPPORTED_FILE_TYPES
from config.constants import CLUSTERING_ALGORITHMS
```

## 扩展指南

### 添加新的模型类型

1. 在 `models/` 下创建新目录（如 `classification/`）
2. 创建模型类，继承 `BaseModel`：

```python
from core.base.model_base import BaseModel
import pandas as pd

class MyModel(BaseModel):
    def fit(self, X: pd.DataFrame, y=None, **kwargs):
        self.validate_data(X, require_y=False)
        # 实现训练逻辑
        self.is_fitted = True
    
    def predict(self, X: pd.DataFrame, **kwargs):
        if not self.is_fitted:
            raise ModelError("模型尚未训练")
        # 实现预测逻辑
        return predictions
```

3. 在 `services/` 下创建对应的服务目录
4. 实现服务类，继承 `BaseService`：

```python
from core.base.service_base import BaseService
import pandas as pd

class MyService(BaseService):
    def execute(self, data: pd.DataFrame, **kwargs):
        self.validate_input(data, **kwargs)
        # 实现服务逻辑
        return result
```

5. 在 `components/` 下创建对应的组件
6. 在 `pages/` 下创建新页面
7. 更新 `config/constants.py` 添加相关常量
8. 在 `config/settings.py` 中添加页面配置

### 添加新的页面

1. 在 `pages/` 下创建新文件
2. 使用 `BasePage` 基类（可选）
3. 在 `config/settings.py` 中添加页面配置
4. 实现页面逻辑

### 添加新的组件

1. 根据功能分类，在 `components/` 对应目录下创建文件
2. 遵循现有的组件设计模式
3. 更新对应的 `__init__.py`

### 添加新功能模块（如分类）的完整步骤

1. **创建模型目录**
   ```bash
   mkdir -p models/classification
   ```

2. **创建服务目录**
   ```bash
   mkdir -p services/classification
   ```

3. **创建组件目录**
   ```bash
   mkdir -p components/classification/{params,analysis,workflows}
   ```

4. **实现模型类** - 继承 `BaseModel`
5. **实现服务类** - 继承 `BaseService`
6. **创建页面** - 在 `pages/` 下创建新文件
7. **更新配置** - 在 `config/` 中添加相关配置

## 最佳实践

1. **使用基类**: 新模型、服务、页面应继承对应的基类
2. **统一异常**: 使用自定义异常类，不要直接抛出通用异常
3. **日志记录**: 重要操作应记录日志
4. **数据验证**: 在服务层进行数据验证
5. **状态管理**: 使用 `StateManager` 管理状态
6. **配置集中**: 所有配置集中在 `config/` 目录

## 未来扩展计划

### 计划添加的功能

1. **特征选择**
   - 单变量特征选择
   - 递归特征消除
   - 特征重要性分析

2. **分类模型**
   - 逻辑回归
   - 随机森林
   - 支持向量机

3. **回归模型**
   - 线性回归
   - 岭回归
   - Lasso 回归

4. **其他功能**
   - 模型评估
   - 模型比较
   - 结果导出

## 依赖包

- streamlit >= 1.28.0
- pandas >= 2.0.0
- scikit-learn >= 1.3.0
- matplotlib >= 3.7.0
- seaborn >= 0.12.0
- numpy >= 1.24.0
- openpyxl >= 3.1.0

## 许可证

MIT License
